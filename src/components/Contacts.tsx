import React from "react";
import { useMediaQuery } from "react-responsive";
import { Element } from "react-scroll";

export default function Contacts() {
  return (
    <Element name="contacts" className="section contacts bg-green-500">
      <h1>Contacts</h1>
    </Element>
  );
}
